spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/my_store_db
    username: coder2client
    password: pastoral2u
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true